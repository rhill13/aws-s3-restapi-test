import { ResolvedWaiterOptions, SmithyClient, WaiterResult } from "./waiter";
/**
 * Function that runs indefinite polling as part of waiters.
 * @param params options passed to the waiter.
 * @param client AWS SDK Client
 * @param input client input
 * @param stateChecker function that checks the acceptor states on each poll.
 */
export declare const runPolling: <T extends SmithyClient, S>({ minDelay, maxDelay, maxWaitTime, abortController, client }: ResolvedWaiterOptions<T>, input: S, acceptorChecks: (client: T, input: S) => Promise<WaiterResult>) => Promise<WaiterResult>;
